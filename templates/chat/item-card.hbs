<div class="chat-card item-card" data-actor-id="{{actor._id}}" data-item-id="{{item._id}}"
     {{#if tokenId}}data-token-id="{{tokenId}}"{{/if}} {{#if isSpell}}data-spell-level="{{item.item.system.level}}"{{/if}}>
    <header class="card-header flexrow">
        <img src="{{item.img}}" title="{{item.name}}" width="36" height="36"/>
        <h3 class="item-name">{{item.name}}</h3>
    </header>
    <div class="card-content">
        {{item.system.attributes.description.value}}
        {{#if simpleRoll}}
            <div class="dice-roll">
                <div class="dice-result">
                    <h4 class="dice-total">
                        {{#if (gt actorRoll.result 0)}}
                            <div class="color-green">{{actorRoll.result}} ({{localize "CHAT.Success"}})</div>
                        {{else if (eq actorRoll.result 0)}}
                            {{actorRoll.result}} ({{localize "CHAT.PartialSuccess"}})
                        {{else}}
                            <div class="color-red">{{actorRoll.result}} ({{localize "CHAT.Failure"}})</div>
                        {{/if}}
                    </h4>
                </div>
            </div>
            <div class="card-stats">
            {{#each actorRoll.attributes as |attribute id|}}
                {{#if (ne attribute.fas undefined)}}
                    <i class="fas {{attribute.fas}} hover-green" data-tooltip="{{localize attribute.label}}">
                        <label>{{attribute.value}}</label>
                    </i>
                {{/if}}
            {{/each}}
        {{else}}

        {{/if}}
    </div>
    <div class="card-buttons">
        <button data-action="apply" data-item-id={{itemId}}>{{localize "CHAT.Apply"}}</button>
        <button data-action="cancel" data-item-id={{itemId}}>{{localize "CHAT.Cancel"}}</button>
    </div>
    <footer class="card-footer">
    </footer>
</div>
