<ol class="items-list">
    <li class="items-header flexrow">
        <div class="item-name">{{localize "PL1E.Weapon"}}</div>
    </li>
    {{#each weapons}}
        <li class="item" data-item-id="{{_id}}">
            <div class="flexrow">
                <img class="item-image item-edit hover-red" src="{{img}}" title="{{name}}" width="24" height="24"/>
                <label class="item-name item-tooltip-activate">{{name}}</label>
                <div class="item-stats grid-span-2">
                    {{#unless (eq @root.actor.type "npc")}}
                        {{#if (ne system.attributes.contactAttackRange 0)}}
                            <i class="item-info fas fa-swords hover-green" data-tooltip="{{localize "PL1E.ContactAttackRange"}}">
                                <label>{{system.attributes.contactAttackRange.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.rangedAttackRange 0)}}
                            <i class="item-info fas fa-bow-arrow hover-green" data-tooltip="{{localize "PL1E.RangedAttackRange"}}">
                                <label>{{system.attributes.rangedAttackRange.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.parryProjectiles 0)}}
                            {{#if system.attributes.parryProjectiles.value}}
                                <i class="item-info fas fa-shield-halved hover-green" data-tooltip="{{localize "PL1E.ParryProjectiles"}}"></i>
                            {{/if}}
                        {{/if}}
                        {{#if (ne system.attributes.slashing 0)}}
                            <i class="item-info fas fa-axe-battle hover-green" data-tooltip="{{localize "PL1E.Slashing"}}">
                                <label>{{system.attributes.slashing.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.crushing 0)}}
                            <i class="item-info fas fa-hammer-war hover-green" data-tooltip='{{localize "PL1E.Crushing"}}'>
                                <label>{{system.attributes.crushing.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.piercing 0)}}
                            <i class="item-info fas fa-dagger hover-green" data-tooltip='{{localize "PL1E.Piercing"}}'>
                                <label>{{system.attributes.piercing.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.parryBonuses 0)}}
                            <i class="item-info fas fa-shield hover-green" data-tooltip='{{localize "PL1E.Parry"}}'>
                                <label>{{system.attributes.parryBonuses.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.hands 0)}}
                            <i class="item-info fas fa-hand hover-green" data-tooltip='{{localize "PL1E.Hands"}}'>
                                <label>{{system.attributes.hands.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.ammo 0)}}
                            <i class="item-info fas fa-tally hover-green" data-tooltip='{{localize "PL1E.Ammo"}}'>
                                <label>{{system.attributes.ammo.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.movementPenalty 0)}}
                            <i class="item-info fas fa-weight-hanging hover-green" data-tooltip='{{localize "PL1E.MovementPenalty"}}'>
                                <label>{{system.attributes.movementPenalty.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.slashingReduction 0)}}
                            <i class="item-info far fa-axe-battle hover-green" data-tooltip='{{localize "PL1E.SlashingReduction"}}'>
                                <label>{{system.attributes.slashingReduction.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.crushingReduction 0)}}
                            <i class="item-info far fa-hammer-war hover-green" data-tooltip='{{localize "PL1E.CrushingReduction"}}'>
                                <label>{{system.attributes.crushingReduction.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.piercingReduction 0)}}
                            <i class="item-info far fa-dagger hover-green" data-tooltip='{{localize "PL1E.PiercingReduction"}}'>
                                <label>{{system.attributes.piercingReduction.value}}</label>
                            </i>
                        {{/if}}
                        {{#if (ne system.attributes.movementPenalty 0)}}
                            <i class="item-info fas fa-weight-hanging hover-green" data-tooltip='{{localize "PL1E.MovementPenalty"}}'>
                                <label>{{system.attributes.movementPenalty.value}}</label>
                            </i>
                        {{/if}}
                    {{/unless}}
                    {{#if (eq @root.actor.type "character")}}
                        {{#if (gt system.price.gold 0)}}
                            <i class="item-info fas fa-coins color-gold hover-green" data-tooltip='{{localize "PL1E.Gold"}}'>
                                <label class="color-black">{{system.price.gold}}</label>
                            </i>
                        {{/if}}
                        {{#if (gt system.price.silver 0)}}
                            <i class="item-info fas fa-coins color-silver hover-green" data-tooltip='{{localize "PL1E.Silver"}}'>
                                <label class="color-black">{{system.price.silver}}</label>
                            </i>
                        {{/if}}
                        {{#if (gt system.price.copper 0)}}
                            <i class="item-info fas fa-coins color-copper hover-green" data-tooltip='{{localize "PL1E.Copper"}}'>
                                <label class="color-black">{{system.price.copper}}</label>
                            </i>
                        {{/if}}
                    {{else if (eq @root.actor.type "merchant")}}
                        {{#with (lookup ../system.merchantPrices _id)}}
                            {{#if (gt gold 0)}}
                                <i class="item-info fas fa-coins color-gold hover-green" data-tooltip='{{localize "PL1E.Gold"}}'>
                                    <label class="color-black">{{gold}}</label>
                                </i>
                            {{/if}}
                            {{#if (gt silver 0)}}
                                <i class="item-info fas fa-coins color-silver hover-green" data-tooltip='{{localize "PL1E.Silver"}}'>
                                    <label class="color-black">{{silver}}</label>
                                </i>
                            {{/if}}
                            {{#if (gt copper 0)}}
                                <i class="item-info fas fa-coins color-copper hover-green" data-tooltip='{{localize "PL1E.Copper"}}'>
                                    <label class="color-black">{{copper}}</label>
                                </i>
                            {{/if}}
                        {{/with}}
                    {{/if}}
                </div>
                <div class="item-controls">
                    {{#if (eq @root.actor.type "merchant")}}
                        <a class="item-control item-buy" data-tooltip='{{localize "PL1E.Buy"}}'>
                            <i class="fas fa-coins"></i>
                        </a>
                    {{else if (or (eq @root.actor.type "character") (eq @root.actor.type "npc"))}}
                        <a class="item-control item-toggle" data-main='false' data-tooltip='{{localize "PL1E.EquipSecondary"}}'>
                            <i class="fas fa-hand icon-flipped {{#unless system.isEquippedSecondary}}color-grey{{/unless}}"></i>
                        </a>
                        <a class="item-control item-toggle" data-main='true' data-tooltip='{{localize "PL1E.EquipMain"}}'>
                            <i class="fas fa-hand {{#unless system.isEquippedMain}}color-grey{{/unless}}"></i>
                        </a>
                    {{/if}}
                    {{#if @root.actor.isOwner}}
                        {{#if (eq flags.core.childId undefined)}}
                            <a class="item-control item-delete" data-tooltip='{{localize "PL1E.Delete"}}'><i class="fas fa-trash"></i></a>
                        {{else}}
                            <a class="item-control item-link" data-tooltip='{{localize "PL1E.Link"}}'><i class="fas fa-link"></i></a>
                        {{/if}}
                    {{/if}}
                </div>
            </div>
            <div class="item-tooltip">{{{system.description}}}</div>
        </li>
    {{/each}}
</ol>