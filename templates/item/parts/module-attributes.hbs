<section class="scroll-auto">
    <div class="item-attribute flexrow">
        <label class="attribute-name">{{localize "PL1E.ItemType"}}</label>
        <select name="system.attributes.itemType">
            {{selectOptions (config "slots") selected=system.attributes.itemType localize=true}}
        </select>
    </div>
    <div class="item-attribute flexrow">
        <label class="attribute-name">{{localize "PL1E.Slot"}}</label>
        <select name="system.attributes.slot">
            {{selectOptions (config "slots") selected=system.attributes.slot localize=true}}
        </select>
    </div>
    {{> "systems/pl1e/templates/item/components/item-aspects-passives.hbs"}}
    {{> "systems/pl1e/templates/item/components/item-aspects-actives.hbs"}}
</section>